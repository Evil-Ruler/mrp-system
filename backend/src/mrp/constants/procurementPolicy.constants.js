const { PROCUREMENT_TYPES } = require("./procurement.constants");

/**
 * Enterprise MRP System Category-to-Procurement Temporary Legacy Compatibility Matrix.
 *
 * Provides a centralized single source of truth for mapping legacy item categories
 * to domain procurement types when explicit procurementType persistence is unconfigured.
 */
const CATEGORY_TO_PROCUREMENT = Object.freeze({
  FINISHED_GOOD: PROCUREMENT_TYPES.PRODUCTION,
  FINISHED_GOODS: PROCUREMENT_TYPES.PRODUCTION,
  FG: PROCUREMENT_TYPES.PRODUCTION,

  PRODUCED_SUB_ASSEMBLY: PROCUREMENT_TYPES.PRODUCTION,
  PRODUCED_SUBASSEMBLY: PROCUREMENT_TYPES.PRODUCTION,
  SUB_ASSEMBLY: PROCUREMENT_TYPES.PRODUCTION,
  SUBASSEMBLY: PROCUREMENT_TYPES.PRODUCTION,
  SA: PROCUREMENT_TYPES.PRODUCTION,

  PURCHASED_SUB_ASSEMBLY: PROCUREMENT_TYPES.PURCHASE,
  PURCHASED_SUBASSEMBLY: PROCUREMENT_TYPES.PURCHASE,

  RAW_MATERIAL: PROCUREMENT_TYPES.PURCHASE,
  RAW_MATERIALS: PROCUREMENT_TYPES.PURCHASE,
  RM: PROCUREMENT_TYPES.PURCHASE,

  CONSUMABLE: PROCUREMENT_TYPES.PURCHASE,
  CONSUMABLES: PROCUREMENT_TYPES.PURCHASE,
  CN: PROCUREMENT_TYPES.PURCHASE,

  HARDWARE: PROCUREMENT_TYPES.PURCHASE,
  HW: PROCUREMENT_TYPES.PURCHASE,
});

module.exports = {
  CATEGORY_TO_PROCUREMENT,
};
