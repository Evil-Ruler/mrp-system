/**
 * @deprecated Legacy stub maintained for backward compatibility.
 * Superseded by engine/bomExplosion.js.
 */
function bomExplosion(itemId, quantity, bomData) {
  return [];
}

module.exports = bomExplosion;
